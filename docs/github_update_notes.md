# GitHub 同步记录

## 2026-06-08 初始整理

首次同步了基础数据处理代码：

- SPAN/GPGGA 与磁强计时间对齐；
- 沿轨一维坐标和 0.5 m 网格建图；
- 4.14/5.13 跨日磁图融合与相似度分析；
- total 场滑窗 NCC 匹配验证；
- 阶段性 Word 报告生成。

## 2026-06-09 研究代码完整同步

本次同步扩展到完整阶段性研究代码：

- HMM/Viterbi 相关实验；
- 轴校准与方向处理实验；
- MSD + ICCP 粗精匹配检查；
- anchor map 构建与前向 anchor HMM；
- 弱 IMU 进度约束与候选筛选；
- ProgressMarginSelector 当前最佳方法；
- 固定滞后、弱里程、端点先验、方向切换等负结果实验；
- 文献对标、SOTA 复现方向、导师汇报报告生成；
- 精选小型 CSV/JSON/PNG 结果文件。

仍然不提交：

- 原始 SPAN 和磁强计数据；
- `magmap_*_aligned_samples.csv` 等数百 MB 对齐样本；
- 大量中间图、临时渲染文件和本地缓存；
- Python 虚拟环境和 `__pycache__`。

当前仓库目的是保留研究路线和可复查代码，不是最终可发布软件包。后续需要进一步模块化和配置化。
